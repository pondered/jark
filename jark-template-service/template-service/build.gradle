group "com.jark.template.service"

dependencies {
    api(project(":common:utils"))
    api("org.mybatis.dynamic-sql:mybatis-dynamic-sql:${MYBATIS_DYNAMIC_VERSION}")
    api("org.mybatis.spring.boot:mybatis-spring-boot-starter:${MYBATIS_SPRING_VERSION}")
}


bootJar {
    enabled = false
}